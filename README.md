# Mobility Data Processing

This repository contains a Python script for processing and analyzing mobility data, focusing on trips across different distance ranges. The script performs data cleaning, analysis, modeling, and visualization using libraries such as `pandas`, `numpy`, `matplotlib`, and `scikit-learn`.

## Installation

Install the required dependencies:

```bash
pip install pandas numpy matplotlib scikit-learn
